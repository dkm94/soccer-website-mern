// import usePagination from 'utils/hooks/usePagination';

// describe('usePagination() function tests', () => {
// 	it('should throw an error if the first argument is not an array', () => {
// 		expect(() => usePagination('something', 123, 123)).toThrow('data should be an array');
// 	});

// 	it('should throw an error if the second argument is not a number or equals 0', () => {
// 		expect(() => usePagination([], 0, 123)).toThrow('itemsPerPage should be a valid number');
// 	});

// 	it('should throw an error if the third argument is not a string', () => {
// 		expect(() => usePagination([], 5, undefined)).toThrow('competition\'s value is invalid');
// 	});
// });