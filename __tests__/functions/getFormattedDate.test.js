// import getFormattedDate from 'utils/getFormattedDate';

// describe('getFormattedDate() function tests', () => {
// 	it('should throw error if first argument is not \'long\' or \'short\' ', () => {
// 		expect(() => getFormattedDate('something', 123)).toThrow('Invalid format');
// 	});

// 	it('should throw error if the second argument is not type string', () => {
// 		expect(() => getFormattedDate('long', undefined)).toThrow('Invalid date type');
// 	});

// 	it('should throw error if the second argument is an empty string', () => {
// 		expect(() => getFormattedDate('long', '')).toThrow('Invalid date type');
// 	});
// });